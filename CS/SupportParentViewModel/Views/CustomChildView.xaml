<UserControl x:Class="SupportParentViewModel.Views.CustomChildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:SupportParentViewModel.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:CustomChildViewModel />
    </UserControl.DataContext>

    <GroupBox Header="Child View with a custom view model" BorderThickness="2">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding ParentViewModel.ParentProperty}" Margin="0 5" />

            <Button Content="Show Message" Command="{Binding ShowMessageCommand}" />
        </StackPanel>
    </GroupBox>
</UserControl>
